<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Dashboard | Syed Music Zone</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <style>
    body {
      background-color: #1DB954;
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
      margin: 0;
      color: white;
    }
    .dashboard-container {
      text-align: center;
      background: rgba(30, 30, 30, 0.85);
      padding: 40px;
      border-radius: 16px;
      max-width: 600px;
      width: 90%;
    }
    h1 {
      font-size: 2em;
      margin-bottom: 20px;
      color: #fff;
    }
    .logout-btn {
      padding: 12px 24px;
      background: #ff4d4d;
      color: white;
      border: none;
      border-radius: 10px;
      cursor: pointer;
      font-size: 1em;
      transition: background 0.3s, transform 0.3s;
    }
    .logout-btn:hover {
      background: #e63946;
      transform: scale(1.1);
    }
    .footer-note {
      margin-top: 20px;
      font-size: 0.85em;
      color: #888;
    }
    .footer-note a {
      color: #1DB954;
      text-decoration: none;
      font-weight: bold;
    }
    .footer-note a:hover {
      text-decoration: underline;
    }
  </style>
</head>
<body>
  <div class="dashboard-container">
    <h1>Welcome, <%= user.username %>!</h1>  <!-- User's username displayed here -->
    <p>You are logged in and ready to explore Syed Music Zone.</p>

    <!-- Logout Button inside Form to Prevent GET request for logout -->
    <form action="/logout" method="POST" style="display: inline;">
      <button type="submit" class="logout-btn">Logout</button>
    </form>

    <div class="footer-note">
      <p>If you want to change your account details, feel free to go to your profile.</p>
    </div>
  </div>
</body>
</html>
